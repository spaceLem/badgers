// performs checks to ensure that nothing bad is happening

#pragma once

#include "defs.hpp"
#include "utils.hpp"

int check(ivec const &N, ivec const &S, ivec const &I, int const last_event, double const t);

